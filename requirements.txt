# Core ML / model-serving (versions inherited from existing dev requirements.txt)
torch>=2.0.0
transformers>=4.35.0
datasets>=2.14.0
accelerate>=0.20.0
compressed-tensors>=0.12.0   # transitively required by transformers to load `RedHatAI/...quantized.w8a8` (DeepSeek-R1-Distill-Llama-70B)

# Stats / numerics / data
numpy>=1.24.0
pandas>=1.5.0
scipy>=1.10.0
statsmodels>=0.14.0

# Plotting
matplotlib>=3.7.0

# Utilities
tqdm>=4.65.0
openpyxl>=3.0.0
python-dotenv>=1.0.0
openai>=1.0.0
